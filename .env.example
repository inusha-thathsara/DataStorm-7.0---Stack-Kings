# Vercel deploy — Gemini + template (no local Ollama)
# Set GEMINI_API_KEY in the Vercel project dashboard (never commit the real key).

NEXT_PUBLIC_OLLAMA_ENABLED=false

GEMINI_API_KEY=your_gemini_key_here
GEMINI_MODEL=gemini-2.5-flash
